{
  "name": "ecommerce-platform",
  "version": "1.0.0",
  "description": "Full-stack e-commerce application built with NestJS, Next.js, and Prisma. It provides a seamless shopping experience with features like product browsing, user authentication, and order management.",
  "main": "index.js",
  "author": "Luca Novello",
  "private": true,
  "license": "UNLICENSED",
  "packageManager": "pnpm@10.32.1",
  "scripts": {
    "docker:up": "docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.yml up -d",
    "docker:down": "docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.yml down",
    "docker:restart": "docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.yml down && docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.yml up -d",
    "docker:logs": "docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.yml logs -f",
    "docker:ps": "docker compose --env-file infra/docker/.env -f infra/docker/docker-compose.yml ps"
  },
  "keywords": [],
  "devDependencies": {
    "@types/node": "^25.5.0",
    "husky": "^9.1.7",
    "lint-staged": "^16.4.0",
    "prettier": "^3.8.1",
    "turbo": "^2.8.17"
  }
}
